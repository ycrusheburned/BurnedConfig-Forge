# Proje sürümü (SemVer). Fabric modül sürümleri fabric.mod.json içinde
# bu değere ek olarak MC etiketi eklenerek türetilir.
mod_version=1.0.0
maven_group=dev.burned
archives_base_name=BurnedConfig

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true

# Forge portu (BurnedConfig-Forge)
org.gradle.daemon=false
